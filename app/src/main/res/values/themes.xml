<resources>
    <!-- Dùng nền sáng cố định (không DayNight) vì màu chữ/thẻ trong layout
         đang đặt cứng theo tông sáng; tự vẽ thanh tiêu đề riêng nên tắt
         ActionBar mặc định. -->
    <style name="Theme.SquareHub" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">#FFFFFF</item>
    </style>

    <style name="Theme.SquareHub.Dialog" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
